# eric/bstm-example-pr

## 2026-07-07

- Added new page `evm/latest/documentation/migrations/gas-converter-migration.mdx` (synced to `next/`), documenting the x/precisebank replacement via a gas converter precompile for 6-decimal chains on v0.7.x. Converted from a standalone draft at the repo root (now removed), applying writing-style rules (no bold in prose, no em-dashes).
- Registered the page in `docs.json` under the EVM Migrations group, directly after the v0.6-to-v0.7 migration page, for both `latest` and `next`.
- Cross-linked the new page from Step 6 (Drop x/precisebank) of the v0.6-to-v0.7 migration guide.
- Added a Warning callout at the top of the gas converter page: example code, not production ready, maintained by adopters not the Cosmos EVM team.
