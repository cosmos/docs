# internal-audit-page

## 2026-06-29

- Added `sdk/latest/security/internal-audits.mdx` (Internal Audit Process), a generalized public writeup of the internal audit methodology. Reports themselves are not published, per team decision.
- Registered the page in `docs.json` under the SDK Security group for both `latest` and `next`.
- Synced to `sdk/next/security/internal-audits.mdx` via `sync-latest-to-next.js`.
