## 2026-05-29

- Rewrote the Enterprise relayer page from an outdated deployment guide into a source-backed overview, and linked readers to the maintained IBC relayer and Proof API docs for operational details.
- Reshaped the Enterprise relayer page to follow the PoA overview pattern more closely, with stronger marketing framing, a benefits table, and an enterprise availability section.
- Updated the relayer comparison table to include an Alternatives column so the page matches the PoA overview structure more closely.
- Added an explicit Key capabilities section so the Enterprise relayer page clearly captures the relayer's core feature set from the upstream README and code.
- Added deck-backed enterprise positioning from the relayer presentation, including the expanded connectivity and features section and the stronger universal IBC relaying comparison table.
- Broadened the relayer opening to position it around universal IBC relaying across Cosmos, EVM, and Solana environments instead of a narrower Cosmos and EVM framing.
- Tightened the relayer overview opening and primary benefits list so the page reads more cleanly as a marketing overview.
- Reordered the Enterprise interoperability navigation so the relayer overview appears first, followed by attestor, with deployment overview last.
- Added a link from the Enterprise relayer page to the Cosmos ↔ EVM interoperability tutorial so readers can see the relayer in a Besu-based demo flow.
- Rewrote the Enterprise attestor page into a marketing overview based on the latest ibc-attestor repo, the attestation light client model, and the enterprise positioning deck, with links to the IBC attestor docs and tutorial walkthrough.
- Updated the Enterprise attestor licensing contact to use the sales inbox in the public docs.
- Expanded the Enterprise attestor page to mirror the slide more closely by naming Besu and L2 environments explicitly, clarifying trust-model composition, and making the cloud and managed-vs-self-hosted deployment options more explicit.
- Removed the Enterprise Network Manager page, dropped it from navigation and component lists, and added a redirect to the Enterprise overview to avoid broken links.
- Renamed the high-level Enterprise labels from Proof of Authority to Permissioned Consensus and from Group to Multi-sig across navigation and overview surfaces.
- Set the Enterprise Components group to stay expanded in the sidebar.
- Added a top-of-page tutorial callout to the Enterprise deployment overview so readers can jump to the working Cosmos ↔ EVM example.
