---
title: "Cosmos Stack Developer Documentation"
mode: "custom"
---

import { TopicCard, TopicCardGrid } from '/snippets/topic-card-component.jsx';

<script>{`
  // Hide navigation elements
  function hideNavElements() {
    const selectors = [
      '.nav-tabs',
      'button[aria-haspopup="menu"]',
      'button[data-state]',
      'header button:not([aria-label*="theme"])'
    ];

    selectors.forEach(selector => {
      try {
        document.querySelectorAll(selector).forEach(el => {
          el.style.setProperty('display', 'none', 'important');
          el.style.setProperty('visibility', 'hidden', 'important');
        });
      } catch(e) {}
    });
  }

  // Theme detection
  function applyTheme() {
    const html = document.documentElement;
    const theme = html.getAttribute('data-theme') ||
                  (html.classList.contains('dark') ? 'dark' : 'light');
    const customContent = document.getElementById('custom-index-content');
    if (customContent) {
      customContent.setAttribute('data-theme', theme);
    }
  }

  // Initialize
  if (typeof window !== 'undefined') {
    hideNavElements();
    applyTheme();

    setTimeout(() => {
      hideNavElements();
      applyTheme();
    }, 100);

    window.addEventListener('load', () => {
      hideNavElements();
      applyTheme();
    });

    // Watch for theme changes
    new MutationObserver(() => applyTheme()).observe(document.documentElement, {
      attributes: true,
      attributeFilter: ['data-theme', 'class']
    });
  }
`}</script>

<style>{`
  /* Theme-aware custom index styles */
  #custom-index-content {
    min-height: 100vh;
    font-family: system-ui, -apple-system, sans-serif;
    transition: background-color 0.3s ease, color 0.3s ease;
  }

  /* Dark theme (default) */
  #custom-index-content[data-theme="dark"],
  #custom-index-content:not([data-theme]) {
    background: #000000;
    color: #F1F1F1;
  }

  /* Light theme */
  #custom-index-content[data-theme="light"] {
    background: #FFFFFF;
    color: #1A1A1A;
  }

  /* Banner styles */
  #custom-index-content[data-theme="dark"] .banner,
  #custom-index-content:not([data-theme]) .banner {
    background: linear-gradient(180deg, #0E0E0E 0%, #000000 100%);
    border-bottom: 1px solid rgba(204, 204, 204, 0.2);
  }

  #custom-index-content[data-theme="light"] .banner {
    background: linear-gradient(180deg, #F5F5F5 0%, #FFFFFF 100%);
    border-bottom: 1px solid rgba(0, 0, 0, 0.1);
  }

  /* Heading colors */
  #custom-index-content[data-theme="dark"] .heading,
  #custom-index-content:not([data-theme]) .heading {
    color: #F1F1F1;
  }

  #custom-index-content[data-theme="light"] .heading {
    color: #1A1A1A;
  }

  /* Section heading underlines */
  #custom-index-content[data-theme="dark"] .heading-underline,
  #custom-index-content:not([data-theme]) .heading-underline {
    background: #FFFFFF !important;
  }

  #custom-index-content[data-theme="light"] .heading-underline {
    background: #000000 !important;
  }


  /* Text colors */
  #custom-index-content[data-theme="dark"] .text,
  #custom-index-content:not([data-theme]) .text {
    color: #CCCCCC;
  }

  #custom-index-content[data-theme="light"] .text {
    color: #4A4A4A;
  }

  /* Mobile responsive styles */
  @media (max-width: 768px) {
    .font-semibold.truncate,
    header button:not([aria-label*="theme"]) {
      display: none !important;
    }

    #custom-index-content .content-container {
      padding: 40px 20px !important;
    }

    #custom-index-content .banner {
      padding: 40px 20px !important;
    }

    #custom-index-content h1 {
      font-size: 32px !important;
    }

    #custom-index-content h2.heading {
      font-size: 28px !important;
    }

    #custom-index-content .content-card {
      padding: 24px !important;
    }
  }

  @media (max-width: 480px) {
    #custom-index-content .content-container,
    #custom-index-content .banner {
      padding: 30px 16px !important;
    }

    #custom-index-content h1 {
      font-size: 28px !important;
    }

    #custom-index-content h2.heading {
      font-size: 24px !important;
    }

    #custom-index-content .content-card {
      padding: 20px !important;
    }
  }

  /* Section borders */
  #custom-index-content[data-theme="dark"] .section-border,
  #custom-index-content:not([data-theme]) .section-border {
    border-bottom: 1px solid rgba(204, 204, 204, 0.1);
  }

  #custom-index-content[data-theme="light"] .section-border {
    border-bottom: 1px solid rgba(0, 0, 0, 0.08);
  }

  /* Card styles */
  #custom-index-content[data-theme="dark"] .content-card,
  #custom-index-content:not([data-theme]) .content-card {
    background: #0E0E0E;
    border: 1px solid rgba(204, 204, 204, 0.2);
  }

  #custom-index-content[data-theme="light"] .content-card {
    background: #F9F9F9;
    border: 1px solid rgba(0, 0, 0, 0.1);
  }

  /* Interactive elements */
  .content-card:hover {
    transform: translateY(-4px);
  }

  .cta-button {
    transition: all 0.2s;
  }

  #custom-index-content[data-theme="dark"] .cta-button,
  #custom-index-content:not([data-theme]) .cta-button {
    background: #000;
    border: 1px solid #CCC;
    color: #F1F1F1;
  }

  #custom-index-content[data-theme="light"] .cta-button {
    background: #FFF;
    border: 1px solid #333;
    color: #1A1A1A;
  }

  .cta-button:hover {
    opacity: 0.9;
  }

  .text-link:hover {
    text-decoration: underline;
  }
`}</style>

<div id="custom-index-content">
  {/* Banner */}
  <div className="banner" style={{
    minHeight: '25vh',
    padding: '60px 40px',
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    justifyContent: 'center',
  }}>
    <h1 className="heading" style={{
      fontSize: '48px',
      fontWeight: '700',
      marginBottom: '16px',
      textAlign: 'center',
    }}>
      Cosmos Stack Developer Documentation
    </h1>
    <p className="text" style={{
      fontSize: '18px',
      maxWidth: '800px',
      textAlign: 'center',
      lineHeight: '1.6',
    }}>
      The most widely-adopted and battle-tested Layer 1 blockchain technology stack, in production with 200+ chains today.
    </p>
  </div>

  {/* Content */}
  <div className="content-container" style={{
    maxWidth: '1200px',
    margin: '0 auto',
    padding: '60px 40px',
  }}>
    {/* Introduction */}
    <section className="section-border" style={{ marginBottom: '80px', paddingBottom: '80px' }}>
      <h2 className="heading" style={{
        fontSize: '32px',
        fontWeight: '700',
        marginBottom: '8px',
      }}>
        Introduction
      </h2>
      <div className="heading-underline" style={{
        width: '66.67%',
        height: '2px',
        background: 'currentColor',
        marginBottom: '20px',
      }}></div>
      <p className="text" style={{
        fontSize: '18px',
        lineHeight: '1.8',
        marginBottom: '16px',
      }}>
        The Cosmos stack is the most widely-adopted and battle-tested Layer 1 blockchain technology stack, in production with <strong>200+ chains</strong> today. It is performant, customizable, and EVM-compatible, offering engineers full end-to-end control of their blockchain infrastructure and implementation.
      </p>
      <p className="text" style={{
        fontSize: '18px',
        lineHeight: '1.8',
      }}>
        The stack is modular by design — leverage pre-existing core components and integrate custom features and logic for your use case, compliance needs, governance, consensus, and performance requirements. This documentation explains the open-source stack components: <strong>Cosmos SDK</strong>, <strong>Cosmos EVM</strong>, <strong>IBC</strong>, and <strong>CometBFT</strong>.
      </p>
    </section>

    {/* Stack Components */}
    <section className="section-border" style={{ marginBottom: '80px', paddingBottom: '80px' }}>
      <h2 className="heading" style={{
        fontSize: '32px',
        fontWeight: '700',
        marginBottom: '8px',
      }}>
        Get Started with the Cosmos Stack
      </h2>
      <div className="heading-underline" style={{
        width: '66.67%',
        height: '2px',
        background: 'currentColor',
        marginBottom: '20px',
      }}></div>
      <p className="text" style={{
        fontSize: '18px',
        lineHeight: '1.8',
        marginBottom: '32px',
      }}>
        Explore the modular components that make up the Cosmos stack. Each component is optional, provides specific functionality and can be customized for your use case.
      </p>

      <TopicCardGrid cols={3}>
        <TopicCard
          title="Cosmos SDK"
          titleHref="/docs/sdk/"
          linkLabel="GO TO DOCS"
          description="Modular framework for building secure, high-performance application-specific blockchains"
          color="#999999"
          links={[
            { text: 'GitHub Repository', href: 'https://github.com/cosmos/cosmos-sdk' },
            { text: 'SDK Overview', href: '/docs/sdk/v0.53/learn/intro/overview' },
            { text: 'Building Modules', href: '/docs/sdk/v0.53/build/building-modules/intro' },
            { text: 'Core Modules', href: '/docs/sdk/v0.53/build/modules/modules' },
            { text: 'Chain Registry', href: 'https://github.com/cosmos/chain-registry' },
            { text: 'Cosmos Network', href: 'https://cosmos.network' },
          ]}
        />

        <TopicCard
          title="Cosmos EVM"
          titleHref="/docs/evm/"
          linkLabel="GO TO DOCS"
          description="EVM compatibility layer with Solidity support and access to Cosmos SDK features"
          color="#aaaaaa"
          links={[
            { text: 'GitHub Repository', href: 'https://github.com/cosmos/evm' },
            { text: 'EVM Overview', href: '/docs/evm/next/documentation/overview' },
            { text: 'Build a Chain', href: '/docs/evm/next/documentation/getting-started/build-a-chain/overview' },
            { text: 'Precompiles', href: '/docs/evm/next/documentation/smart-contracts/precompiles/overview' },
            { text: 'JSON-RPC API', href: '/docs/evm/next/api-reference/ethereum-json-rpc/rpc-explorer' },
          ]}
        />

        <TopicCard
          title="IBC Protocol"
          titleHref="/docs/ibc/"
          linkLabel="GO TO DOCS"
          description="Inter-Blockchain Communication protocol for secure cross-chain transfers and messaging"
          color="#888888"
          links={[
            { text: 'GitHub Repository', href: 'https://github.com/cosmos/ibc-go' },
            { text: 'IBC Protocol Site', href: 'https://ibc.cosmos.network' },
            { text: 'IBC Overview', href: 'https://ibc.cosmos.network' },
            { text: 'ICS-20 Token Transfers', href: '/docs/ibc/ics20' },
            { text: 'Interchain Accounts', href: '/docs/ibc/interchain-accounts' },
          ]}
        />

        <TopicCard
          title="CometBFT"
          titleHref="https://docs.cometbft.com/v0.38/"
          linkLabel="GO TO DOCS"
          description="Byzantine Fault Tolerant consensus engine achieving up to 10,000 TPS with instant finality"
          color="#cccccc"
          links={[
            { text: 'GitHub Repository', href: 'https://github.com/cometbft/cometbft' },
            { text: 'CometBFT Documentation', href: 'https://docs.cometbft.com' },
            { text: 'ABCI++ Specification', href: 'https://docs.cometbft.com/v0.38/spec/abci/' },
            { text: 'Consensus Overview', href: 'https://docs.cometbft.com/v0.38/explanation/core/consensus' },
          ]}
        />

        <TopicCard
          title="Development Tools"
          description="Essential tools and frameworks for building and deploying on the Cosmos stack"
          color="#777777"
          links={[
            { text: 'InterchainTest', href: 'https://github.com/cosmos/interchaintest' },
            { text: 'InterchainJS', href: 'https://docs.hyperweb.io/interchain-js' },
            { text: 'Skip API', href: 'https://docs.skip.build/go/api-reference/prod/info/get-v2infochains' },
            { text: 'Skip Widget', href: 'https://docs.skip.build/go/general/getting-started' },
            { text: 'EVM Testing & Fuzzing', href: '/docs/evm/next/documentation/getting-started/tooling-and-resources/testing-and-fuzzing' },
          ]}
        />

        <TopicCard
          title="Getting Started"
          description="Quick start guides and tutorials to begin building with the Cosmos stack, and common EVM tools"
          color="#666666"
          links={[
            { text: 'Quick Start', href: 'https://docs.cosmos.network/v0.53/build' },
            { text: 'Local Node Setup', href: 'https://docs.cosmos.network/v0.53/user/run-node/run-production' },
            { text: 'REST API', href: 'https://docs.cosmos.network/api' },
            { text: 'EVM JSON RPC', href: '/docs/evm/next/api-reference/ethereum-json-rpc/rpc-explorer' },
          ]}
        />
      </TopicCardGrid>
    </section>

    {/* About Sections */}
    <section className="section-border" style={{ marginBottom: '80px', paddingBottom: '80px' }}>
      <h2 className="heading" style={{
        fontSize: '32px',
        fontWeight: '700',
        marginBottom: '8px',
      }}>
        About the Stack
      </h2>
      <div className="heading-underline" style={{
        width: '66.67%',
        height: '2px',
        background: 'currentColor',
        marginBottom: '20px',
      }}></div>

      <div
        id="cosmos-sdk"
        className="content-card"
        style={{
          borderRadius: '12px',
          padding: '40px',
          marginBottom: '24px',
          transition: 'all 0.3s ease',
          transform: 'translateY(0)',
          cursor: 'default',
        }}
        onMouseEnter={(e) => {
          const card = e.currentTarget;
          const theme = document.getElementById('custom-index-content').getAttribute('data-theme');
          const isLight = theme === 'light';
          if (isLight) {
            card.style.background = '#E5E5E5';
          } else {
            card.style.background = 'rgba(255, 255, 255, 0.08)';
          }
          card.style.transform = 'translateY(-4px)';
        }}
        onMouseLeave={(e) => {
          const card = e.currentTarget;
          card.style.background = '';
          card.style.transform = 'translateY(0)';
        }}
      >
        <h3
          className="heading"
          style={{
            fontSize: '24px',
            fontWeight: '600',
            marginBottom: '16px',
          }}
        >
          Cosmos SDK
        </h3>
        <p className="text" style={{
          fontSize: '16px',
          lineHeight: '1.7',
          marginBottom: '12px',
        }}>
          The Cosmos SDK is a modular, open-source blockchain SDK for building secure, high-performance Layer 1 chains with full customizability. Developers can use the Cosmos SDK to easily and quickly spin up custom blockchains that can natively interoperate via a native integration with the Inter-Blockchain Communication Protocol.
        </p>
        <p className="text" style={{
          fontSize: '16px',
          lineHeight: '1.7',
        }}>
          The Cosmos SDK is tailored for building secure, sovereign application-specific blockchains. Developers building with the Cosmos SDK can use <strong>predefined modules</strong> that cover standard blockchain functionality or <strong>create custom modules</strong> for their specific use case. This composable architecture enables robust customization. The SDK provides abstractions for permissioning, governance, state management, account abstraction, tokenization processes, application logic, and more.
        </p>
      </div>

      <div
        id="cosmos-evm"
        className="content-card"
        style={{
          borderRadius: '12px',
          padding: '40px',
          marginBottom: '24px',
          transition: 'all 0.3s ease',
          transform: 'translateY(0)',
          cursor: 'default',
        }}
        onMouseEnter={(e) => {
          const card = e.currentTarget;
          const theme = document.getElementById('custom-index-content').getAttribute('data-theme');
          const isLight = theme === 'light';
          if (isLight) {
            card.style.background = '#E5E5E5';
          } else {
            card.style.background = 'rgba(255, 255, 255, 0.08)';
          }
          card.style.transform = 'translateY(-4px)';
        }}
        onMouseLeave={(e) => {
          const card = e.currentTarget;
          card.style.background = '';
          card.style.transform = 'translateY(0)';
        }}
      >
        <h3
          className="heading"
          style={{
            fontSize: '24px',
            fontWeight: '600',
            marginBottom: '16px',
          }}
        >
          Cosmos EVM
        </h3>
        <p className="text" style={{
          fontSize: '16px',
          lineHeight: '1.7',
        }}>
          Cosmos EVM is a plug-and-play solution that adds EVM compatibility and customizability to Cosmos SDK chains. It provides complete Ethereum capabilities: Solidity smart contracts, Ethereum JSON-RPC, native support for the EVM wallet/token/user experience, and access to the entire Ethereum developer ecosystem. Its <strong>precompiles and extensions</strong> allow developers to leverage modules like IBC within the EVM and get native ERC-20 support for tokens on Cosmos. It can run any valid smart contract from Ethereum and also implement new features that are not yet available on the standard Ethereum VM.
        </p>
      </div>

      <div
        id="ibc-protocol"
        className="content-card"
        style={{
          borderRadius: '12px',
          padding: '40px',
          marginBottom: '24px',
          transition: 'all 0.3s ease',
          transform: 'translateY(0)',
          cursor: 'default',
        }}
        onMouseEnter={(e) => {
          const card = e.currentTarget;
          const theme = document.getElementById('custom-index-content').getAttribute('data-theme');
          const isLight = theme === 'light';
          if (isLight) {
            card.style.background = '#E5E5E5';
          } else {
            card.style.background = 'rgba(255, 255, 255, 0.08)';
          }
          card.style.transform = 'translateY(-4px)';
        }}
        onMouseLeave={(e) => {
          const card = e.currentTarget;
          card.style.background = '';
          card.style.transform = 'translateY(0)';
        }}
      >
        <h3
          className="heading"
          style={{
            fontSize: '24px',
            fontWeight: '600',
            marginBottom: '16px',
          }}
        >
          IBC Protocol
        </h3>
        <p className="text" style={{
          fontSize: '16px',
          lineHeight: '1.7',
        }}>
          The Inter-Blockchain Communication protocol (IBC) is a blockchain interoperability solution that allows blockchains to talk to each other. Blockchains that speak IBC can transfer any kind of data encoded in bytes—including tokens, messages, and arbitrary application logic. IBC is <strong>secure, permissionless, and designed to connect sovereign blockchains</strong> into a single interoperable network.
        </p>
      </div>

      <div
        id="cometbft"
        className="content-card"
        style={{
          borderRadius: '12px',
          padding: '40px',
          transition: 'all 0.3s ease',
          transform: 'translateY(0)',
          cursor: 'default',
        }}
        onMouseEnter={(e) => {
          const card = e.currentTarget;
          const theme = document.getElementById('custom-index-content').getAttribute('data-theme');
          const isLight = theme === 'light';
          if (isLight) {
            card.style.background = '#E5E5E5';
          } else {
            card.style.background = 'rgba(255, 255, 255, 0.08)';
          }
          card.style.transform = 'translateY(-4px)';
        }}
        onMouseLeave={(e) => {
          const card = e.currentTarget;
          card.style.background = '';
          card.style.transform = 'translateY(0)';
        }}
      >
        <h3
          className="heading"
          style={{
            fontSize: '24px',
            fontWeight: '600',
            marginBottom: '16px',
          }}
        >
          CometBFT
        </h3>
        <p className="text" style={{
          fontSize: '16px',
          lineHeight: '1.7',
          marginBottom: '12px',
        }}>
          CometBFT is the most widely-adopted, battle-tested consensus engine in blockchain. It is a Byzantine Fault Tolerant (BFT) middleware that takes a state transition machine—written in any programming language—and securely replicates it on many machines.
        </p>
        <p className="text" style={{
          fontSize: '16px',
          lineHeight: '1.7',
        }}>
          CometBFT is highly performant and achieves speeds of up to <strong>10,000 transactions per second (TPS)</strong>. Its flagship feature, <strong>ABCI++</strong>, enables developers to add programmability and customization to every step of the consensus engine. Developers can use CometBFT for BFT state machine replication of applications written in any programming language and development environment. It is a fork of Tendermint.
        </p>
      </div>
    </section>

    {/* Maintainers and Contributors */}
    <section style={{ marginBottom: '60px' }}>
      <h2 className="heading" style={{
        fontSize: '32px',
        fontWeight: '700',
        marginBottom: '8px',
      }}>
        Maintainers and Contributors
      </h2>
      <div className="heading-underline" style={{
        width: '66.67%',
        height: '2px',
        background: 'currentColor',
        marginBottom: '24px',
      }}></div>

      <div className="content-card" style={{
        borderRadius: '12px',
        padding: '40px',
      }}>
        <p className="text" style={{
          fontSize: '16px',
          lineHeight: '1.7',
          marginBottom: '16px',
        }}>
          <strong>Cosmos Labs</strong> maintains the core components of the stack: Cosmos SDK, CometBFT, IBC, Cosmos EVM, and various developer tools and frameworks. In addition to developing and maintaining the Cosmos Stack, Cosmos Labs provides advisory and engineering services for blockchain solutions.
        </p>
        <p className="text" style={{
          fontSize: '16px',
          lineHeight: '1.7',
          marginBottom: '16px',
        }}>
          <a href="https://cosmoslabs.io/" target="_blank" rel="noopener noreferrer" className="text-link" style={{ textDecoration: 'underline' }}>Cosmos Labs</a> is a wholly-owned subsidiary of the <a href="https://interchain.io/" target="_blank" rel="noopener noreferrer" className="text-link" style={{ textDecoration: 'underline' }}><strong>Interchain Foundation</strong></a>, the Swiss nonprofit responsible for treasury management, funding public goods, and supporting governance for Cosmos.
        </p>
        <p className="text" style={{
          fontSize: '16px',
          lineHeight: '1.7',
          marginBottom: '24px',
        }}>
          The Cosmos Stack is supported by a global community of open-source contributors.
        </p>

        <a
          href="https://www.cosmoslabs.io/contact"
          target="_blank"
          rel="noopener noreferrer"
          className="cta-button"
          style={{
            display: 'inline-block',
            padding: '14px 28px',
            borderRadius: '8px',
            textDecoration: 'none',
            fontWeight: '600',
            transition: 'all 0.2s',
          }}
        >
          Get in Touch with Cosmos Labs →
        </a>
      </div>
    </section>
  </div>
</div>
