---
title: "Counter"
description: "gRPC queries and transaction messages defined by the counter module in Cosmos SDK v0.55 (unreleased)."
---

{/* Generated by scripts/api-reference/sync-sdk-api-reference.js. Do not edit by hand. */}

<Info>
  Generated from [`cosmos/cosmos-sdk@b8d4c467735a`](https://github.com/cosmos/cosmos-sdk/tree/b8d4c467735a8895b450886ba6af1eb0414a9725/proto) on ref `main`, documenting Cosmos SDK v0.55 (unreleased).

  A chain exposes only the modules its application registers, so it may serve a subset of what appears here alongside methods of its own. Query a running node with reflection for the authoritative list.
</Info>

## Queries

These methods are defined as gRPC services. A node serves the ones its application registers, on the gRPC server on port 9090, and on the REST gateway on port 1317 where a method carries an HTTP binding. A service defined here is not necessarily one a node answers: `grpcurl -plaintext localhost:9090 list` says what a given node actually serves.

Examples are templates: replace every `<string>` placeholder with a real value, and drop optional filter fields you do not want to filter on. Payloads here are protobuf JSON, where a `cosmos.Dec` is the scaled integer string rather than a decimal. A `string` field may take an enum constant, listed under Types. Examples assume a local node; against a TLS endpoint drop `-plaintext` and use port 443. See [gRPC services](/sdk/next/api-reference/grpc/index) for reflection recipes, address formats, and how decimal values are encoded.

### GetCount

GetCount queries the parameters of x/Counter module.

| | |
| --- | --- |
| gRPC | `cosmos.counter.v1.Query/GetCount` |

Request `QueryGetCountRequest`:

This message has no fields.

Response `QueryGetCountResponse`:

| Field | Type | Description |
| --- | --- | --- |
| `total_count` | `int64` |  |

```bash
grpcurl -plaintext localhost:9090 cosmos.counter.v1.Query/GetCount
```

## Transaction messages

These are not callable endpoints. Each one is encoded into a transaction, signed, and broadcast. See [Sending Transactions](/sdk/next/api-reference/transactions) for how to build, sign, and broadcast one. The handler name is shown for reference and for generated clients, not as an address you can call directly.

The JSON below is transaction JSON, the form the CLI writes and reads and the form `POST /cosmos/tx/v1beta1/encode` accepts. A `cosmos.Dec` field there is a plain decimal string such as `"0.05"`. This holds even where the field's proto type is `bytes`: the base64 form is how such a field reads back over gRPC, not how it is written. The same field passed to the `cosmos.tx.v1beta1.Service/TxEncode` gRPC method instead needs the scaled integer string `"50000000000000000"`, because that path uses a different codec. See [scalar encodings](/sdk/next/api-reference/grpc/index#scalar-encodings).

The signer names the field holding the account whose key must sign the transaction. The Amino name identifies the message under `SIGN_MODE_LEGACY_AMINO_JSON`, the signing mode used by signers that display a transaction for human approval, such as hardware wallets. It is frozen for signature compatibility, so it often does not follow from the message name.

### IncreaseCount

IncreaseCount increments the counter by the specified amount.

| | |
| --- | --- |
| Type URL | `/cosmos.counter.v1.MsgIncreaseCounter` |
| Handler | `cosmos.counter.v1.Msg/IncreaseCount` |
| Signer | `signer` |
| Amino name | `cosmos-sdk/increase_counter` |

| Field | Type | Description |
| --- | --- | --- |
| `signer` | `string` | signer is the address that controls the module (defaults to x/gov unless overwritten). Encoded as cosmos.AddressString. |
| `count` | `int64` | count is the number of times to increment the counter. |

In a transaction:

```json
{
  "@type": "/cosmos.counter.v1.MsgIncreaseCounter",
  "signer": "<string>",
  "count": "1"
}
```

Response `MsgIncreaseCountResponse`:

| Field | Type | Description |
| --- | --- | --- |
| `new_count` | `int64` | new_count is the number of times the counter was incremented. |


